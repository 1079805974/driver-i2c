#include "wclock.ceu"
#include "i2c.ceu"

emit I2C(on);
loop do
    var u8 i;
    loop i in [0 -> 4] do
        twi_transmit_buffer = twi_transmit_buffer .. [i];
    end
    emit I2C_REQUEST_SEND(4);
    await I2C_REQUEST_DONE;
end