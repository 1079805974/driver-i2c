{Serial.begin(9600);}
#include "i2c.ceu"
emit I2C(on);
loop do
    emit I2C_REQUEST_RECEIVE(4,2);
    await I2C_REQUEST_DONE;
    var u8 i;
    loop i in [1-> $twi_receive_buffer as u8] do
        {Serial.print(@twi_receive_buffer[i-1]);}
    end
    {Serial.println("bye");}
    //await 1s;
end